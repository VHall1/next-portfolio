import { Flex } from "@chakra-ui/core";
import React from "react";

export const NavBar: React.FC<{}> = ({}) => {
  return (
    <Flex>
      <div className="logo">{"<Victor Hall />"}</div>
      
    </Flex>
  );
};
